# LTSC Golden Image Builder

Комплексная система подготовки эталонного образа Windows 10 LTSC с автоматизацией первого входа пользователя.

Полностью автоматизирует создание "золотого образа" через Sysprep и настройку профилей новых пользователей.

## ⚠️ КРИТИЧЕСКИ ВАЖНО!

**НЕ ЗАПУСКАЙ НА СВОЕЙ РАБОЧЕЙ СИСТЕМЕ!**

Этот скрипт предназначен **ТОЛЬКО** для запуска в **Audit Mode** (режим аудита).

### Как правильно использовать:

1. **Установи чистую Windows 10 LTSC**
2. **При первом экране OOBE нажми `Ctrl + Shift + F3`**
   - Система перезагрузится в Audit Mode
   - Ты войдёшь как встроенный Администратор
3. **Настрой систему в Audit Mode**
   - Установи программы
   - Настрой интерфейс
   - Запусти этот скрипт
4. **Sysprep выключит систему**
5. **Захвати образ**

### Почему именно Audit Mode?

- ✅ Профиль встроенного Администратора копируется в Default User
- ✅ Все настройки применятся ко всем новым пользователям
- ✅ Это официальный способ создания корпоративных образов Microsoft

### Что будет если запустить на рабочей системе?

- ❌ **Sysprep удалит все пользовательские данные**
- ❌ **Система станет "обобщённой" и потребует повторной активации**
- ❌ **Все установленные программы могут перестать работать**
- ❌ **Профиль пользователя будет повреждён**

**Используй только на чистой тестовой системе в Audit Mode!**

---

## 🎯 Что это

Двухэтапная система для создания корпоративного образа Windows 10 LTSC:

**Этап 1:** Подготовка системы → Sysprep → создание образа  
**Этап 2:** Автоматическая настройка каждого нового пользователя при первом входе

**Возможности:**
- 🧹 Полная очистка системы от персональных данных
- 🎨 Настройка профиля по умолчанию (аватар пользователя, расположение плиток в меню Пуск)
- 🤖 Автопереименование компьютера при первом входе
- 📁 Автоматическая настройка Быстрого доступа для новых пользователей
- 🔧 Исправление путей в настройках программ для нового пользователя
- 🎭 Установка кастомных иконок для папок

**Философия проекта:**

Используются **только официально задокументированные методы Microsoft**:
- Audit Mode — стандартный режим для OEM-производителей
- Sysprep с CopyProfile — официальный способ копирования профиля
- unattend.xml — стандартный механизм автоматизации развёртывания
- Папка `C:\Windows\Setup\Scripts\` — официальное место для deployment-скриптов

**Это не "сборка от Васи Пупкина"** — это профессиональный образ, подготовленный так, как это делают корпорации и OEM-производители. После использования вы получите **чистый образ Windows**, который выглядит так, будто Microsoft подготовила его специально для вас.

## 📦 Содержимое

```
LTSC_Golden_Image_Builder/
├── README.md
├── Clean_and_Sysprep.ps1          # Основной скрипт подготовки к Sysprep
├── Pictures_profile/
│   └── user.png                   # Аватар пользователя
└── StartUP/
    ├── FirstRun.vbs               # Невидимый запуск из автозагрузки
    └── Scripts/
        ├── FirstRun.ps1           # Исправление путей в настройках программ
        └── SetupQuickAccess.ps1   # Настройка Быстрого доступа и иконок
```

## 🔄 Как это работает

### Механизм работы (подробно)

Система работает в **два этапа**: подготовка образа и настройка новых пользователей.

---

### **ЭТАП 1: Подготовка образа (перед Sysprep)**

#### Что делает администратор вручную:

**Шаг 1. Настройка системы**
- Установи все нужные программы (⚠️ **только .exe/.msi**, НЕ Appx из Microsoft Store!)
- Настрой интерфейс как надо
- Расставь плитки в меню Пуск

**Шаг 2. Подготовка автозагрузки**
- Скопируй `FirstRun.vbs` в автозагрузку Администратора:
  ```
  %APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\
  ```
- Когда Sysprep скопирует профиль Администратора в Default User, этот файл попадёт в автозагрузку каждого нового пользователя

**Шаг 3. Размещение скриптов**
- Скопируй `FirstRun.ps1` и `SetupQuickAccess.ps1` в:
  ```
  C:\Windows\Setup\Scripts\
  ```
- Это официальная папка Microsoft для deployment-скриптов
- Создай папку `Scripts` если её нет

**Шаг 4. Запуск главного скрипта**
- Запусти `Clean_and_Sysprep.ps1` от имени администратора
- Выбери режим `[1] - Полная подготовка + Sysprep`

#### Что делает Clean_and_Sysprep.ps1:

**[1/9] Очистка кэша браузеров**
- Chrome, Edge, Yandex — кеш и Code Cache

**[2/9] Очистка временных файлов**
- %LOCALAPPDATA%\Temp
- INetCache
- Recent (недавние файлы)

**[3/9] Удаление логов и дампов**
- Все .log файлы в AppData
- CrashDumps
- Миниатюры (thumbcache)

**[4/9] Очистка истории**
- История адресной строки Explorer
- История Win+R

**[5/9] Настройка плиток меню Пуск**
- Экспортирует текущую раскладку плиток в `LayoutModification.xml`
- Сохраняет в `xml_for_build\LayoutModification.xml`
- Копирует в Default User профиль
- Новые пользователи получат эти же плитки

**[6/9] Скрытие лупы поиска**
- Скрывает поиск на панели задач в Default User профиле

**[7/9] Установка аватара**
- Копирует `user.png` в `C:\ProgramData\Microsoft\User Account Pictures\`
- Применяет ко всем новым пользователям

**[8/9] Генерация FirstLogon.ps1**
- Создаёт скрипт автопереименования компьютера
- Сохраняет в `xml_for_build\FirstLogon.ps1`
- При первом входе переименует ПК в `USERNAME-PC` (где USERNAME — имя пользователя, которое вы введёте при установке Windows)

**[9/9] Генерация unattend.xml**
- Создаёт файл автоматизации OOBE
- Включает запуск FirstLogon.ps1 при первом входе
- Включает `<CopyProfile>true</CopyProfile>` — копирование профиля Администратора в Default User

**Финал:**
- Копирует `FirstLogon.ps1` в `C:\Windows\Setup\Scripts\`
- Запускает Sysprep с параметрами:
  ```
  sysprep.exe /generalize /oobe /shutdown /unattend:"путь\unattend.xml"
  ```
- Компьютер выключается — образ готов к захвату!

---

### **ЭТАП 2: Первый вход нового пользователя (после установки Windows)**

После установки Windows из образа происходит **двухступенчатая настройка**:

#### A) Этап OOBE (синее окно приветствия и настройки)

**OOBE** (Out-Of-Box Experience) — это синее окно приветствия при первой установке Windows, где вы выбираете язык, создаёте пользователя и настраиваете систему.

**Что происходит на этом этапе:**

Файл `unattend.xml` отрабатывает **в невидимом режиме** (вы ничего не увидите) и автоматически запускает скрипт `C:\Windows\Setup\Scripts\FirstLogon.ps1`.

**Действия скрипта FirstLogon.ps1:**
1. Windows запускает скрипт в фоновом режиме
2. Скрипт переименовывает компьютер в `USERNAME-PC`
   - Где USERNAME — это имя пользователя, которое вы только что ввели при настройке
3. Скрипт удаляет сам себя
4. Вы видите экран входа в систему

**Почему SetupQuickAccess.ps1 НЕ запускается здесь:**
- На этапе OOBE оболочка Explorer ещё не полностью инициализирована
- Перезапуск Explorer работает нестабильно (через раз)
- Настройка Быстрого доступа может не примениться

**Поэтому настройка интерфейса отложена на следующий этап.**

#### B) При старте каждого нового пользователя (из папки автозагрузки)

Запускается **автоматически** при первом входе каждого нового пользователя в систему.

**Что происходит:**

1. **Новый пользователь получает из Default User:**
   - Все настройки профиля Администратора
   - Плитки меню Пуск
   - Аватар
   - **FirstRun.vbs в автозагрузке** ← скопировался автоматически!

2. **Запускается `FirstRun.vbs`** (невидимо, без окна):
   ```vbs
   CreateObject("Wscript.Shell").Run "powershell -WindowStyle Hidden ...", 0, False
   ```

3. **FirstRun.vbs запускает `C:\Windows\Setup\Scripts\FirstRun.ps1`**

4. **FirstRun.ps1 исправляет пути:**
   
   Заменяет "Администратор" → текущее имя пользователя в настройках программ:
   
   - **PotPlayer:**
     ```
     %APPDATA%\PotPlayerMini64\PotPlayerMini64.ini
     ```
   
   - **qBittorrent:**
     ```
     %APPDATA%\qBittorrent\qBittorrent.ini
     %APPDATA%\qBittorrent\watched_folders.json
     ```
   
   *Почему это нужно:* Профиль скопирован из Администратора, поэтому все пути содержат `C:\Users\Администратор\`. Нужно заменить на `C:\Users\НовыйПользователь\`.

5. **FirstRun.ps1 запускает `C:\Windows\Setup\Scripts\SetupQuickAccess.ps1`**

6. **SetupQuickAccess.ps1 настраивает интерфейс:**

   **[Шаг 1] Иконка папки пользователя**
   - Создаёт `desktop.ini` в `%USERPROFILE%`
   - Устанавливает иконку `shell32.dll,158`
   
   **[Шаг 2] Создание папки TORRENTS**
   - Создаёт папку `%USERPROFILE%\TORRENTS`
   - Создаёт скрытую подпапку `temp`
   - Устанавливает иконку из `qbittorrent.exe`
   
   **[Шаг 3] Обновление иконок**
   - Вызывает Shell API для обновления иконок
   - Перезапускает Explorer для применения изменений
   - **Это работает!** Explorer уже полностью загружен
   
   **[Шаг 4] Открепление стандартных папок**
   - Открепляет из Быстрого доступа:
     - Рабочий стол
     - Документы
     - Загрузки
     - Изображения
     - Музыка
     - Видео
   
   **[Шаг 5] Закрепление своих папок**
   - Закрепляет в нужном порядке:
     1. Папка пользователя
     2. Рабочий стол
     3. Загрузки
     4. TORRENTS
     5. Документы
     6. Изображения

7. **Очистка:**
   - FirstRun.ps1 удаляет `FirstRun.vbs` из автозагрузки
   - Сами скрипты `FirstRun.ps1` и `SetupQuickAccess.ps1` остаются в системе

**Результат:**
- Компьютер переименован в `USERNAME-PC`
- Настройки программ исправлены под нового пользователя
- Быстрый доступ настроен с кастомными папками
- Иконки папок применены
- Всё работает идеально, потому что Explorer уже запущен!

---

## 🚀 Использование

### ⚠️ Обязательно: Вход в Audit Mode

**Перед началом работы:**

1. **Установи чистую Windows 10 LTSC** (с ISO образа)

2. **При первом экране OOBE (выбор языка/региона):**
   - Нажми **`Ctrl + Shift + F3`** одновременно
   - Система перезагрузится
   - Автоматически войдёшь как **Администратор**
   - Откроется окно Sysprep — **закрой его (НЕ запускай!)**

3. **Ты в Audit Mode!** Теперь можно настраивать систему

**Что такое Audit Mode:**
- Специальный режим Windows для подготовки образов
- Работа от имени встроенного Администратора
- Профиль этого Администратора станет шаблоном для всех новых пользователей
- Официальный способ Microsoft для создания корпоративных образов

---

### Подготовка образа

**1. Установи и настрой Windows**
```
- Установи все нужные программы
  ⚠️ ВАЖНО: НЕ устанавливай Appx-приложения из Microsoft Store!
  Они вызовут ошибку Sysprep. Используй только классические .exe/.msi программы.
- Настрой интерфейс
- Расставь плитки в меню Пуск
```

**2. Подготовь автозагрузку**
```
Скопируй: StartUP\FirstRun.vbs
     → В: %APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\
```

**3. Размести скрипты**
```
Скопируй: StartUP\Scripts\FirstRun.ps1
     → В: C:\Windows\Setup\Scripts\

Скопируй: StartUP\Scripts\SetupQuickAccess.ps1
     → В: C:\Windows\Setup\Scripts\
```

**4. Запусти главный скрипт**
```powershell
# От имени администратора
.\Clean_and_Sysprep.ps1

# Выбери режим: [1] - Полная подготовка + Sysprep
```

**5. Дождись выключения**
```
Система автоматически:
- Очистится
- Настроит Default User
- Сгенерирует файлы
- Запустит Sysprep
- Выключится
```

**6. Захвати образ**
```
Используй DISM, ImageX или любой другой инструмент
для создания WIM/ISO образа
```

---

### Тестовый режим

Если хочешь только проверить генерацию файлов без запуска Sysprep:

```powershell
.\Clean_and_Sysprep.ps1

# Выбери режим: [2] - ТЕСТ: Только генерация файлов
```

**Будет создано:**
- `xml_for_build\FirstLogon.ps1`
- `xml_for_build\unattend.xml`

Без изменений системы и без запуска Sysprep.

---

## 📁 Генерируемые файлы

### xml_for_build/FirstLogon.ps1

Автоматически генерируется Clean_and_Sysprep.ps1.

**Функции:**
- Переименовывает компьютер в `USERNAME-PC`
- Удаляет сам себя после выполнения

**Запуск:**
- Автоматически при OOBE (из unattend.xml)

---

### xml_for_build/unattend.xml

Автоматически генерируется Clean_and_Sysprep.ps1.

**Содержит:**
- `<CopyProfile>true</CopyProfile>` — копирование профиля Администратора
- Запуск FirstLogon.ps1 при первом входе

**Используется:**
- Sysprep при создании образа

---

### xml_for_build/LayoutModification.xml

Автоматически генерируется Clean_and_Sysprep.ps1.

**Содержит:**
- Экспортированную раскладку плиток меню Пуск
- Применяется ко всем новым пользователям

**Копируется в:**
- `C:\Users\Default\AppData\Local\Microsoft\Windows\Shell\`

---

## 💡 Что настраивается автоматически

### Для всех новых пользователей (из Default User)

- ✅ Аватар пользователя (из `Pictures_profile\user.png`)
- ✅ Плитки меню Пуск (текущая раскладка)
- ✅ Панель задач (скрыта лупа поиска)
- ✅ Все настройки профиля Администратора

### При первом входе пользователя (автоматически)

- ✅ Переименование ПК в `USERNAME-PC`
- ✅ Исправление путей в PotPlayer
- ✅ Исправление путей в qBittorrent
- ✅ Иконка папки пользователя
- ✅ Создание папки TORRENTS с иконкой
- ✅ Настройка Быстрого доступа
- ✅ Перезапуск Explorer для применения изменений

---

## ⚙️ Настройка под себя

### Изменить аватар

Замени файл:
```
Pictures_profile\user.png
```

Требования:
- Формат: PNG
- Размер: любой (скрипт скопирует под все размеры)

---

### Изменить программы для исправления путей

Отредактируй `StartUP\Scripts\FirstRun.ps1`:

```powershell
# Добавь свою программу по аналогии:
$myApp = "$env:APPDATA\MyApp\config.ini"
if (Test-Path $myApp) {
    $content = [IO.File]::ReadAllText($myApp, [Text.Encoding]::UTF8)
    $content = $content -replace 'Администратор', $env:USERNAME
    [IO.File]::WriteAllText($myApp, $content, [Text.Encoding]::UTF8)
}
```

---

### Изменить папки в Быстром доступе

Отредактируй `StartUP\Scripts\SetupQuickAccess.ps1`:

```powershell
# Найди раздел "Закрепление папок"
$PathsToPin = @(
    $env:USERPROFILE,           # Папка пользователя
    (Join-Path $env:USERPROFILE "Desktop"),
    (Join-Path $env:USERPROFILE "Downloads"),
    (Join-Path $env:USERPROFILE "TORRENTS"),
    (Join-Path $env:USERPROFILE "Documents"),
    (Join-Path $env:USERPROFILE "Pictures")
)
```

Измени список на свой.

---

## 🔧 Технические детали

### Почему FirstRun.vbs в автозагрузке, а не FirstRun.ps1?

**Проблема:**
- PowerShell скрипты в автозагрузке показывают окно консоли
- Это выглядит непрофессионально

**Решение:**
- VBS запускает PowerShell с флагом `-WindowStyle Hidden`
- Окно полностью невидимо

---

### Почему SetupQuickAccess.ps1 запускается из автозагрузки, а не из unattend.xml?

**Проблема:**
- На этапе OOBE Explorer ещё не полностью инициализирован
- Перезапуск Explorer работает нестабильно (через раз)
- Настройки Быстрого доступа могут не примениться

**Решение:**
- Запуск после входа пользователя
- Explorer уже полностью загружен
- Перезапуск работает стабильно
- Иконки и Быстрый доступ применяются без проблем

**Проверено:**
- Неделя тестирования показала что это единственный надёжный метод

---

### Почему FirstRun.ps1 и SetupQuickAccess.ps1 не удаляются?

**Причина:**
- Они лежат в системной папке `C:\Windows\Setup\Scripts\`
- Нужны для каждого нового пользователя
- Удаляется только `FirstRun.vbs` из автозагрузки конкретного пользователя

---

### Формат desktop.ini

Файлы `desktop.ini` должны быть строго в **Unicode** кодировке:

```ini
[.ShellClassInfo]
IconFile=%SystemRoot%\System32\shell32.dll
IconIndex=158
ConfirmFileOp=0
```

**Важно:**
- Кодировка: Unicode (UTF-16 LE)
- Атрибуты файла: Hidden, System
- Атрибут папки: ReadOnly

Скрипт SetupQuickAccess.ps1 делает это автоматически.

---

### Почему происходит перезапуск Explorer при первом входе?

**Техническая необходимость:**
- Для применения кастомных иконок папок через `desktop.ini` требуется обновление оболочки Explorer
- Shell API (`SHChangeNotify`) не всегда срабатывает без полного перезапуска процесса
- Настройка Быстрого доступа через COM-объекты также требует перезагрузки Explorer

**Это абсолютно безопасно:**
- Перезапуск выполняется через стандартные команды Windows: `Stop-Process -Name explorer`
- Explorer автоматически перезапускается системой
- Пользователь видит моргание экрана на 1-2 секунды — это нормально
- Все открытые окна и программы остаются работать

**Почему не использовать другие способы:**
- Попытки настроить Быстрый доступ через реестр — не работают стабильно
- Попытки применить настройки через GPO — требуют домена
- Попытки обойтись без перезапуска — иконки и закрепления применяются "через раз"

**Проверено неделей тестирования:** Перезапуск Explorer при первом входе — единственный надёжный метод для гарантированного применения всех настроек интерфейса.

---

## ⚠️ Важные замечания

### Перед Sysprep

- ✅ Убедись что `FirstRun.vbs` в автозагрузке Администратора
- ✅ Убедись что скрипты лежат в `C:\Windows\Setup\Scripts\`
- ✅ Проверь что `user.png` на месте
- ✅ Настрой плитки меню Пуск как надо

### После Sysprep

- ⚠️ НЕ входи в систему! Компьютер должен выключиться
- ⚠️ Сразу захватывай образ после выключения
- ⚠️ Если войдёшь — придётся готовить заново

### При установке образа

- ✅ Первый пользователь автоматически станет администратором
- ✅ Все настройки применятся автоматически
- ✅ Компьютер переименуется в `USERNAME-PC`
- ✅ Быстрый доступ настроится
- ✅ Иконки применятся

---

## 📝 Чеклист подготовки образа

**Перед началом:**

- [ ] Установлена чистая Windows 10 LTSC
- [ ] Вошёл в Audit Mode (Ctrl+Shift+F3 при OOBE)
- [ ] Работаю от имени встроенного Администратора

**Перед запуском Clean_and_Sysprep.ps1:**

- [ ] Установлены все нужные программы
- [ ] Настроен интерфейс как надо
- [ ] Плитки меню Пуск расставлены
- [ ] `FirstRun.vbs` скопирован в автозагрузку Администратора
- [ ] `FirstRun.ps1` скопирован в `C:\Windows\Setup\Scripts\`
- [ ] `SetupQuickAccess.ps1` скопирован в `C:\Windows\Setup\Scripts\`
- [ ] `user.png` на месте в `Pictures_profile\`
- [ ] Запускаю `Clean_and_Sysprep.ps1` от имени администратора
- [ ] Выбрал режим `[1]`
- [ ] Компьютер выключился
- [ ] Захватил образ БЕЗ входа в систему

---

## 🎓 Глоссарий

**Sysprep** — System Preparation Tool, инструмент Microsoft для подготовки Windows к клонированию/развёртыванию

**OOBE** — Out-Of-Box Experience, экран первоначальной настройки Windows (выбор языка, создание пользователя и т.д.)

**Default User** — профиль-шаблон, из которого копируются настройки для всех новых пользователей

**unattend.xml** — файл автоматизации установки Windows, позволяет запускать команды на разных этапах установки

**CopyProfile** — опция Sysprep, копирует профиль текущего пользователя в Default User

**Golden Image** — эталонный образ Windows с предустановленными программами и настройками для корпоративного развёртывания

---

## 🔗 Похожие проекты

- [LTSC_MS-Store_2021_Installer](https://github.com/NaitSide/LTSC_MS-Store_2021_Installer) — установка Microsoft Store в LTSC
- [LTSC_Media_Extensions_Installer](https://github.com/NaitSide/LTSC_Media_Extensions_Installer) — установка медиа-расширений (AV1, HEVC, WebP и др.)
- [LTSC_System_Apps_Installer](https://github.com/NaitSide/LTSC_System_Apps_Installer) — установка базовых UWP-приложений (Калькулятор, Часы)

---

## 👤 Автор

**NaitSide** — 2026

---

## 📄 Лицензия

Скрипты распространяются "как есть" без гарантий.  
Используйте на свой страх и риск.

---

## 🤝 Поддержка

Если скрипт помог — поставь ⭐ на GitHub!

Нашёл баг или есть предложение? Открой Issue.